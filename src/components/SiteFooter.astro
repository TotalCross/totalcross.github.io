---
import logo from "../assets/logo-totalcross.png";
import SocialIcon from "./SocialIcon.astro";

const groups = [
  {
    title: "Resources",
    links: [["Get started", "/get-started/"], ["Docs", "https://learn.totalcross.com/"], ["Contribute", "https://github.com/TotalCross/totalcross"], ["Enterprise", "http://www.superwaba.net/SDKRegistrationService/"], ["Services", "/services/"]],
  },
  {
    title: "Community",
    links: [["GitHub", "https://github.com/TotalCross/totalcross"], ["YouTube", "https://bit.ly/youtube-totalcross"], ["Medium", "https://medium.com/totalcross-community"]],
  },
  {
    title: "About us",
    links: [["Our history", "/about-us/"], ["Contact us", "/contact/"]],
  },
];

const social = [
  { label: "GitHub", name: "github", href: "https://github.com/TotalCross/totalcross" },
  { label: "X (Twitter)", name: "twitter", href: "https://twitter.com/TotalCross" },
  { label: "Telegram", name: "telegram", href: "https://t.me/totalcrosscommunity" },
  { label: "YouTube", name: "youtube", href: "https://bit.ly/youtube-totalcross" },
  { label: "Instagram", name: "instagram", href: "https://www.instagram.com/totalcross/" },
  { label: "LinkedIn", name: "linkedin", href: "https://linkedin.com/company/totalcross" },
] as const;
---
<!--
SPDX-FileCopyrightText: 2026 Amalgam Solucoes em TI Ltda.
SPDX-License-Identifier: MIT
-->


<footer class="site-footer">
  <div class="footer-inner container">
    <div class="groups">
      {groups.map((group) => (
        <section>
          <h2>{group.title}</h2>
          <ul>
            {group.links.map(([label, href]) => <li><a href={href}>{label}</a></li>)}
          </ul>
        </section>
      ))}
    </div>
    <div class="brand">
      <img src={logo.src} alt="TotalCross" width="180" height="40" />
      <div class="social" aria-label="Social networks">
        {social.map(({ label, name, href }) => (
          <a href={href} aria-label={label} title={label} target="_blank" rel="noopener noreferrer">
            <SocialIcon name={name} />
          </a>
        ))}
      </div>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--color-ink);
    color: var(--color-white);
    padding-block: 2.5rem;
  }

  .footer-inner,
  .groups,
  .social {
    display: flex;
  }

  .footer-inner {
    justify-content: space-between;
  }

  .groups {
    gap: clamp(2rem, 5vw, 5rem);
  }

  h2 {
    border-bottom: 1px solid var(--color-white);
    font-size: 1rem;
    font-weight: 500;
    margin: 0 0 1rem;
    padding-bottom: 0.5rem;
  }

  ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  li + li {
    margin-top: 0.5rem;
  }

  li a {
    font-size: 0.85rem;
    text-decoration: none;
  }

  li a:hover,
  li a:focus-visible {
    color: var(--color-brand-green);
  }

  .brand {
    align-items: flex-end;
    display: flex;
    flex-direction: column;
    gap: 1.4rem;
    padding-top: 0.25rem;
  }

  .brand img {
    height: 40px;
    object-fit: contain;
    width: 180px;
  }

  .social {
    gap: 0.65rem;
  }

  .social a {
    align-items: center;
    border: 1px solid rgb(255 255 255 / 55%);
    border-radius: 50%;
    color: var(--color-white);
    display: inline-flex;
    font-size: 0.65rem;
    height: 28px;
    justify-content: center;
    text-decoration: none;
    text-transform: uppercase;
    width: 28px;
  }

  .social a:hover,
  .social a:focus-visible {
    border-color: var(--color-brand-green);
    color: var(--color-brand-green);
    transform: scale(1.12);
  }

  @media (max-width: 760px) {
    .footer-inner,
    .groups {
      align-items: center;
      flex-direction: column;
    }

    .groups {
      text-align: center;
      width: 100%;
    }

    .brand {
      align-items: center;
      margin-top: 2rem;
    }
  }
</style>
